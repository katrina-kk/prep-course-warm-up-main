//TASK DONE
export {};

function trim(word: string) {
  return word.trim();
}

const result = trim("CODELEX  ");
console.log(result); // Expected output: "CODELEX"

/* original 
function trim(word) {
  return "word".trim();
}

const result = trim("CODELEX  ");
console.log(result); // Expected output: "CODELEX"*/
